apiVersion: networking.k8s.io/v1
kind: gateway
metadata:
  name: abhishek-gateway
  namespace: abhishek-namespace
spec:
  gatewayClassName: kgateway
  listeners:
    - name: http
      port: 80
      protocol: HTTP
      allowedRoutes:
        namespaces:
          from: same
          
